--- a/sound/soc/soc-utils.c
+++ b/sound/soc/soc-utils.c
@@ -29,6 +29,7 @@ int snd_soc_ret(const struct device *dev
 	case -EPROBE_DEFER:
 	case -ENOTSUPP:
 	case -EOPNOTSUPP:
+	case -ENXIO: /* ADTN/D2 patch: ignore device/address errors. Only the pcm part, not the control part, is used */
 		break;
 	default:
 		va_start(args, fmt);
