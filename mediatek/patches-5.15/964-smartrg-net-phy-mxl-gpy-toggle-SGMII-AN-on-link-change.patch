From: Chad Monroe <chad@monroe.io>
Date: Thu, 27 Apr 2023 17:00:17 -0700
Subject: [PATCH]  net: phy: mxl: toggle SGMII AN on link change 

Sometimes the GPY211 can come up and report all is well but no RX packets
are received by the MAC. For some reason toggling the SGMII AN bit clears
this condition. As a very hacky workaround, enable it during physical
link change and let gpy_update_interface disable it shortly after. 

Signed-off-by: Chad Monroe <chad@monroe.io>
---
 drivers/net/phy/mxl-gpy.c |   40 ++++++++++++++++++++++++++++++++++++
 1 file changed, 40 insertions(+)

--- a/drivers/net/phy/mxl-gpy.c
+++ b/drivers/net/phy/mxl-gpy.c
@@ -332,6 +332,8 @@ static int gpy_config_aneg(struct phy_de
 	if (phydev->state != PHY_UP)
 		return 0;
 
+	phydev_info(phydev, "gpy_config_aneg: trigger VSPEC1_SGMII_CTRL_ANRS\n");
+
 	ret = phy_read_poll_timeout(phydev, MII_BMSR, ret, ret & BMSR_LSTATUS,
 				    20000, 4000000, false);
 	if (ret == -ETIMEDOUT)
@@ -344,6 +346,32 @@ static int gpy_config_aneg(struct phy_de
 			      VSPEC1_SGMII_CTRL_ANRS, VSPEC1_SGMII_CTRL_ANRS);
 }
 
+/**
+ * in some cases the GPY211 will link up but not RX packets
+ * 
+ * setting SGMII AN to enabled then back to disabled clears this
+ * for some reason. enable SGMII AN here and let the update
+ * interface poller clear it shortly after. 
+ */
+static void gpy_link_change_notify(struct phy_device *phydev)
+{
+	int ret;
+
+	phydev_info(phydev, "gpy_link_change_notify phydev->state=%d\n", phydev->state);
+
+//	if (phydev->state != PHY_RUNNING)
+//		return;
+
+	ret = phy_read_mmd(phydev, MDIO_MMD_VEND1, VSPEC1_SGMII_CTRL);
+	phydev_info(phydev, "gpy_link_change_notify VSPEC1_SGMII_CTRL=%04x set ANEN\n", ret);
+
+	ret = phy_set_bits_mmd(phydev, MDIO_MMD_VEND1, VSPEC1_SGMII_CTRL,
+			     VSPEC1_SGMII_CTRL_ANEN);
+
+	ret = phy_read_mmd(phydev, MDIO_MMD_VEND1, VSPEC1_SGMII_CTRL);
+	phydev_info(phydev, "gpy_link_change_notify VSPEC1_SGMII_CTRL=%04x DONE\n", ret);
+}
+
 static void gpy_update_interface(struct phy_device *phydev)
 {
 	int ret;
@@ -612,6 +640,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 	{
 		.phy_id		= PHY_ID_GPY115B,
@@ -630,6 +659,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy115_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 	{
 		PHY_ID_MATCH_MODEL(PHY_ID_GPY115C),
@@ -647,6 +677,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy115_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 	{
 		.phy_id		= PHY_ID_GPY211B,
@@ -665,6 +696,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 	{
 		PHY_ID_MATCH_MODEL(PHY_ID_GPY211C),
@@ -682,6 +714,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 	{
 		.phy_id		= PHY_ID_GPY212B,
@@ -700,6 +733,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 	{
 		PHY_ID_MATCH_MODEL(PHY_ID_GPY212C),
@@ -717,6 +751,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 	{
 		.phy_id		= PHY_ID_GPY215B,
@@ -735,6 +770,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 	{
 		PHY_ID_MATCH_MODEL(PHY_ID_GPY215C),
@@ -752,6 +788,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 	{
 		PHY_ID_MATCH_MODEL(PHY_ID_GPY241B),
@@ -769,6 +806,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 	{
 		PHY_ID_MATCH_MODEL(PHY_ID_GPY241BM),
@@ -786,6 +824,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 	{
 		PHY_ID_MATCH_MODEL(PHY_ID_GPY245B),
@@ -803,6 +842,7 @@ static struct phy_driver gpy_drivers[] =
 		.set_wol	= gpy_set_wol,
 		.get_wol	= gpy_get_wol,
 		.set_loopback	= gpy_loopback,
+		.link_change_notify	= gpy_link_change_notify,
 	},
 };
 module_phy_driver(gpy_drivers);
