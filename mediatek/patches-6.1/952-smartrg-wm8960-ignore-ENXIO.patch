--- a/sound/soc/soc-component.c
+++ b/sound/soc/soc-component.c
@@ -26,6 +26,7 @@ static inline int _soc_component_ret(str
 	switch (ret) {
 	case -EPROBE_DEFER:
 	case -ENOTSUPP:
+	case -ENXIO: /* ADTN/D2 patch: ignore device/address errors. Only the pcm part, not the control part, is used */
 		break;
 	default:
 		if (reg == -1)
