--- a/drivers/net/phy/aquantia/aquantia_main.c
+++ b/drivers/net/phy/aquantia/aquantia_main.c
@@ -856,9 +856,9 @@ static int aqr107_probe(struct phy_devic
 	if (!phydev->priv)
 		return -ENOMEM;
 
-	ret = aqr_firmware_load(phydev);
-	if (ret)
-		return ret;
+	//ret = aqr_firmware_load(phydev);
+	//if (ret)
+	//	return ret;
 
 	return aqr_hwmon_probe(phydev);
 }
