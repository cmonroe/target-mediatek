Index: linux-5.10.120/sound/soc/soc-component.c
===================================================================
--- linux-5.10.120.orig/sound/soc/soc-component.c
+++ linux-5.10.120/sound/soc/soc-component.c
@@ -24,6 +24,7 @@ static inline int _soc_component_ret(str
 	switch (ret) {
 	case -EPROBE_DEFER:
 	case -ENOTSUPP:
+	case -ENXIO: /* ADTN/D2 patch: ignore device/address errors. Only the pcm part, not the control part, is used */
 		break;
 	default:
 		dev_err(component->dev,
